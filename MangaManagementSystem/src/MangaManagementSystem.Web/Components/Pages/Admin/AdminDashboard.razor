@page "/admin"
@using Microsoft.AspNetCore.Authorization
@using MangaManagementSystem.Web.Services.Api
@attribute [Authorize(Roles = "Admin")]
@inject NavigationManager Nav
<<<<<<< HEAD
@layout AdminLayout
@inject IUserService UserService
=======
@inject IAdminUserApiClient AdminUserApiClient
>>>>>>> main

    <div class="mf-page">
        <div class="mf-page-header">
            <MudText Typo="Typo.h3"
                     Class="font-weight-bold mb-1">
                System Administration Dashboard
            </MudText>
            <MudText Typo="Typo.body1"
                     Class="mf-muted">
                Monitor and manage MangaFlow platform operations
            </MudText>
        </div>

        <MudText Typo="Typo.overline"
                 Class="mf-muted">
            Account Management
        </MudText>

        <MudPaper Class="pa-4 mf-card mb-4">
            @if (_isLoading)
            {
                <MudProgressLinear Indeterminate="true" />
            }
            else if (_loadError != null)
            {
                <MudAlert Severity="Severity.Error">
                    Failed to load admin stats: @_loadError
                </MudAlert>
            }
            else
            {
                <MudGrid Class="mb-3">
                    <MudItem xs="12" sm="6" md="3">
                        <StatCard Label="Pending Approvals"
                                  Value="@_pendingCount.ToString()" />
                        <MudButton Variant="Variant.Text"
                                   Size="Size.Small"
                                   Href="/admin/user-approval">
                            Manage
                        </MudButton>
                    </MudItem>

                    <MudItem xs="12" sm="6" md="3">
                        <StatCard Label="Active Users"
                                  Value="@_activeCount.ToString()" />
                        <MudButton Variant="Variant.Text"
                                   Size="Size.Small"
                                   Href="/admin/users">
                            Manage
                        </MudButton>
                    </MudItem>

                    <MudItem xs="12" sm="6" md="3">
                        <StatCard Label="Disabled Users"
                                  Value="@_disabledCount.ToString()" />
                        <MudButton Variant="Variant.Text"
                                   Size="Size.Small"
                                   Href="/admin/users">
                            Manage
                        </MudButton>
                    </MudItem>

                    <MudItem xs="12" sm="6" md="3">
                        <StatCard Label="Rejected Users"
                                  Value="@_rejectedCount.ToString()" />
                        <MudButton Variant="Variant.Text"
                                   Size="Size.Small"
                                   Href="/admin/users">
                            Manage
                        </MudButton>
                    </MudItem>
                </MudGrid>
            }
        </MudPaper>

        <MudText Typo="Typo.overline"
                 Class="mf-muted">
            Review &amp; Traceability
        </MudText>

        <MudGrid Class="mb-4">
            <MudItem xs="12" md="4">
                <WorkspaceCard Title="Manage User Approvals"
                               Description="Review and approve pending account requests."
                               Href="/admin/user-approval" />
            </MudItem>

            <MudItem xs="12" md="4">
                <WorkspaceCard Title="Manage User Accounts"
                               Description="Lock, unlock, disable, and manage user access."
                               Href="/admin/users" />
            </MudItem>

            <MudItem xs="12" md="4">
                <WorkspaceCard Title="Audit Logs"
                               Description="Review and filter system audit events by action, entity type, and date."
                               Href="/admin/audit-logs" />
            </MudItem>
        </MudGrid>

        <MudGrid Class="mb-4">
            <MudItem xs="12" md="4">
                @if (_hasFileManagement)
                {
                    <WorkspaceCard Title="File Management"
                                   Description="Manage system file resources."
                                   Href="/admin/files" />
                }
                else
                {
                    <WorkspaceCard Title="File Management"
                                   Description="Not available yet." />
                }
            </MudItem>

            <MudItem xs="12" md="4">
</MudItem>
        </MudGrid>

        <MudText Typo="Typo.overline"
                 Class="mf-muted">
            System Shortcuts
        </MudText>

        <MudGrid>
            <MudItem xs="12" md="3">
                <MudPaper Class="pa-3 mf-card text-center">
                    <MudText Typo="Typo.subtitle2">
                        Accounts
                    </MudText>
                    <MudText Class="mf-muted">
                        User lifecycle &amp; access
                    </MudText>
                </MudPaper>
            </MudItem>

            <MudItem xs="12" md="3">
                <MudPaper Class="pa-3 mf-card text-center">
                    <MudText Typo="Typo.subtitle2">
                        Portfolios
                    </MudText>
                    <MudText Class="mf-muted">
                        Pending portfolio approvals
                    </MudText>
                </MudPaper>
            </MudItem>

            <MudItem xs="12" md="3">
                <MudPaper Class="pa-3 mf-card text-center">
                    <MudText Typo="Typo.subtitle2">
                        Files
                    </MudText>
                    <MudText Class="mf-muted">
                        Storage &amp; CDN
                    </MudText>
                </MudPaper>
            </MudItem>
        </MudGrid>
    </div>

@code {
    private int _pendingCount;
    private int _activeCount;
    private int _disabledCount;
    private int _rejectedCount;
    private bool _isLoading;
    private string? _loadError;
    private bool _hasFileManagement;

    protected override async Task OnInitializedAsync()
    {
        _hasFileManagement = true;
        await LoadStatsAsync();
    }

    private async Task LoadStatsAsync()
    {
        _isLoading = true;
        _loadError = null;

        try
        {
            var counts =
                await AdminUserApiClient
                    .GetStatusCountsAsync();

            _pendingCount =
                counts.PendingApproval;

            _activeCount =
                counts.Active;

            _disabledCount =
                counts.Disabled;

            _rejectedCount =
                counts.Rejected;
        }
        catch (Exception ex)
        {
            _loadError = ex.Message;
        }
        finally
        {
            _isLoading = false;
        }
    }
}
